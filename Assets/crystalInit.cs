using UnityEngine;
using System.Collections;

public class crystalInit : MonoBehaviour {
	public static int gameSize;
	public GameObject ShieldCrystal;
	public GameObject  pulseCrystal;

	// Use this for initialization;
	void Start() {
		for (float  i = 0; i < 50; i += .2) {
			float amplitue = Random.Range(4, 15);
				//Vector2 initSpot = 
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
